# Runtime install for Prefect Cloud managed execution (`--with-requirements requirements.txt`).
# Editable so `Path(__file__).parents[3]` in churn/flows/mlflow.py still resolves to the repo root
# rather than into site-packages. Notebook-only packages live in the `eda` dependency group,
# which pip ignores by design — see pyproject.toml. Local dev uses `uv sync`, not this file.
-e .
